# ask-render reference

For `/polycli:ask --provider minimax`, primary payload is `response`.

Render the response verbatim. Do not mention logs; current MiniMax uses `mmx text chat --output json --non-interactive` and does not produce a Mini-Agent log path.
