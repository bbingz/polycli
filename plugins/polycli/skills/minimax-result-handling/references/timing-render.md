# timing-render reference

`/polycli:timing --provider minimax` only reads Polycli timing history.

MiniMax remains `ephemeral` for runtime persistence and has no fine-grained TTFT/gen/tail/tool timing.
